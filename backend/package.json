{
  "name": "spur-ai-support-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:setup": "node --env-file-if-exists=.env --experimental-strip-types src/db/setup.ts",
    "dev": "node --env-file-if-exists=.env --experimental-strip-types --watch src/server.ts",
    "start": "node --env-file-if-exists=.env --experimental-strip-types src/server.ts",
    "check": "node --experimental-strip-types --check src/server.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
